# M00 - Architecture foundation: evidence record

**Verdict: ACCEPTED (primary pass, no repair pass used).** Each exit criterion below is backed by the command output in `artifacts/evidence/M00/`. The evidence labels follow master §22.3.

## Repository state at start (SOURCE-INSPECTED)

| Item | Value |
|---|---|
| Root | `/Users/andrew/Civic_Godstorm` (existing Git repository; not reinitialised) |
| Branch | `main`, clean working tree |
| HEAD | `5bd357e14d9b68776d50b1dee0829b7bd5ab939f` "docs: establish Civic Godstorm production architecture" |
| Remote | `origin git@github.com:westkitty/Civic_Godstorm.git` (fetch and push). This is the SSH form of the target URL |
| Fetched remote | `origin/main` = `5bd357e`. No divergence and no other remote branches |
| Tracked files | The three planning documents only. No implementation existed, so there was no conflict with the greenfield plan |
| Work branch | `m00-architecture-foundation`, created from verified `main` |

## Reconciliation (SCHEMA/TYPE-VERIFIED)

- Section 17 SHA-256 is `6b4bf909…c80`. The master and Arena copies are identical. The signed span is the `## 17.` heading through the `<!-- CG_ASSET_CONTRACT_END -->` marker plus a newline.
- The milestone block SHA-256 is `3cabfe70…01`. The master and build-prompt copies are identical. The signed span is the Section 19 block, trimmed, plus a newline.

## Exit criteria

| Criterion (master §19, M00) | Result | Evidence |
|---|---|---|
| Repo, branch and remotes described | Done | Table above |
| Actual tool versions pinned | Node 24.21.0 LTS, npm 11.19.0, exact dependency versions and lockfile. A clean `npm ci` from the lockfile succeeded | `docs/TOOLCHAIN.md`, `npm-ci.log`, `environment.json`, `license-inventory.json` |
| Schema compilation verifies 285 IDs and exact counts | 48/48 contract checks pass: signatures, IDs, paths, class/profile/directory counts, filename rule, dependency closure and acyclicity, 28-system register and traceability, atlas, 13-batch allocation, 16 Godform mappings | `spec-compile.log`, `spec-check.log`, `test-assets.log` |
| Checks can fail | 9 corrupted-catalog fixtures each trip their targeted check (UNIT-EXECUTED) | `tests/tools/contract.test.ts`, `test-unit.log` |
| Typecheck and build pass | Both exit 0. Shell bundle is 795 KB raw / 208 KB gzip, under the 4 MiB compressed-shell budget | `typecheck.log`, `build.log` |
| Accessible browser boot and error path | 8/8 Playwright journeys pass in Chrome 153 at 1440x900 and 800x1100 (BROWSER-EXECUTED): boot with nonblank WebGL2 render, labelled sections without overflow, `WEBGL2_UNAVAILABLE` alert with focused heading and a keyboard-reachable retry, `RENDERER_START_FAILED` alert with recovery, no page errors | `test-e2e.log` |
| Lint enforces determinism boundaries | A throwaway `src/sim/lint_probe.ts` using `three`, `Math.random`, `Date.now`, `new Date()` and `window` produced 5 errors; the probe was then deleted | Command recorded below |
| Visual check of shell | Desktop and narrow screenshots reviewed. Placeholders are unmistakable and carry literal `MISSING <ID>` labels | `shell-desktop-1440x900.png`, `shell-narrow-800x1100.png` |

Lint probe command, run once and not committed:

```bash
npx eslint src/sim/lint_probe.ts
```

It reported `no-restricted-imports`, `no-restricted-properties` twice, `no-restricted-syntax` and `no-restricted-globals`.

## Command surface results (final clean run)

| Command | Exit | Meaning |
|---|---|---|
| typecheck | 0 | pass |
| lint | 0 | pass |
| test:unit | 0 | 4 files, 30 tests |
| test:sim | 1 | "No test files found". This is the honest result, because the kernel is M01 scope |
| test:assets | 0 | 55 checks pass. Completion: authored 0/121 approved, derived 0/112 verified, recipes 0/52 registered, 285 unresolved |
| test:campaigns | 2 | not implemented. Arguments are validated, and bad input exits 64 (`test-campaigns-usage.log`) |
| bench | 2 | not implemented (M12) |
| build | 0 | pass |
| test:e2e | 0 | 8 passed |
| validate:release | 1 | correct: 285 required IDs unresolved and 0/121 sources approved |

## Findings recorded, not acted on

- **Rows outside the traceability closure.** Eight IDs are not reachable from any §17.10 traceability row: the four `CG-D-GOD-FORM-*` fixtures (validation fixtures by design) and `CG-R-FX-DUST`, `CG-R-FX-SPLASH`, `CG-R-FX-SPORES` and `CG-R-FX-HEAT`. Every row still declares consumers, and SYS-RENDER covers "all declared visual recipes", so this is not a contract violation. It is listed in `specification.json.outsideTraceabilityClosure` for human review.
- **Chunk-size warning.** Vite warns about a chunk over 500 kB (`three` plus React in one chunk). This is informational and within budget. Code-splitting is an M12 performance concern, not an M00 defect.
- **Skipped install script.** npm 11 skipped the optional `fsevents` install script. It is only a macOS file-watch accelerator and is not used by builds or tests.
- **Dirty build identity.** Screenshot and e2e runs predate the M00 commit, so the build identity shows `5bd357e14d9b-dirty`.

## Not claimed

- No simulation, gameplay, God control, persistence, audio or AI exists.
- No asset has been generated, approved, derived or verified, and Opus generated no source art.
- No cross-browser matrix, physical tablet, performance measurement or Blender conversion was exercised.
- Nothing was pushed or deployed.
